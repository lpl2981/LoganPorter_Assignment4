//- list.pug
extends layout
block content
    h1= title
    div
        nav.pre-scrollable
             each val in notes
                ul
                    li
                        a(href='/api/notes/' + val._id) val.Notes.Title
    a.btn.btn-primary.btn-lg(href="/notes" role="button") Add New Note

    footer.footer
        p CIS 385
